<!DOCTYPE html>
<html lang="pt-br">
<head>
<meta charset="UTF-8">
<meta name="viewport" content="width=device-width, initial-scale=1.0">
<title>Pomodoro ENEM e Concursos - Timer de Estudo Gratuito</title>
<style>
body {
    font-family: Arial, sans-serif;
    text-align: center;
    background-color: #f4f6f9;
    padding: 40px;
}
h1 {
    color: #222;
}
#timer {
    font-size: 60px;
    margin: 20px 0;
}
button {
    padding: 10px 20px;
    margin: 5px;
    font-size: 16px;
    cursor: pointer;
}
.content {
    max-width: 700px;
    margin: 40px auto;
    text-align: left;
}
</style>
</head>
<body>

<h1>Pomodoro ENEM e Concursos</h1>
<div id="timer">25:00</div>
<button onclick="startTimer()">Iniciar</button>
<button onclick="pauseTimer()">Pausar</button>
<button onclick="resetTimer()">Resetar</button>

<div class="content">
<h2>Como usar o método Pomodoro para ENEM e Concursos</h2>
<p>O método Pomodoro é uma técnica de estudo baseada em blocos de foco de 25 minutos seguidos por 5 minutos de descanso.</p>
<p>Essa estratégia aumenta a concentração e ajuda na retenção do conteúdo, sendo muito utilizada por estudantes que se preparam para o ENEM e concursos públicos.</p>
<p>Use este timer gratuito para organizar sua rotina de estudos e melhorar sua produtividade.</p>
</div>

<script>
let time = 1500;
let timerInterval;

function updateDisplay() {
    let minutes = Math.floor(time / 60);
    let seconds = time % 60;
    document.getElementById("timer").innerText =
        (minutes < 10 ? "0" : "") + minutes + ":" +
        (seconds < 10 ? "0" : "") + seconds;
}

function startTimer() {
    if (!timerInterval) {
        timerInterval = setInterval(() => {
            if (time > 0) {
                time--;
                updateDisplay();
            }
        }, 1000);
    }
}

function pauseTimer() {
    clearInterval(timerInterval);
    timerInterval = null;
}

function resetTimer() {
    clearInterval(timerInterval);
    timerInterval = null;
    time = 1500;
    updateDisplay();
}

updateDisplay();
</script>

</body>
</html>